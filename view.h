#pragma once
#include <SFML/Graphics.hpp>
using namespace sf;

//View view2(sf::FloatRect(0, 0, 1000, 600));;
/*void setPlayerCoordForView(float x, float y)
{
	float tempX = x, tempY = y;

	if (tempY > 624) tempY = 624;
	view.setCenter(tempX + 100, tempY);
}

void viewMap(float time)
{
	if (Keyboard::isKeyPressed(Keyboard::Right)) view.move(0.1 * time, 0);
	else if (Keyboard::isKeyPressed(Keyboard::Down)) view.move(0, 0.1 * time);
	else if (Keyboard::isKeyPressed(Keyboard::Left)) view.move(-0.1 * time, 0);
	else if (Keyboard::isKeyPressed(Keyboard::Up)) view.move(0, -0.1 * time);
}*/